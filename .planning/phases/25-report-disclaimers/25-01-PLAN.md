---
phase: 25-report-disclaimers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/workspace/report-workspace.tsx
autonomous: true

must_haves:
  truths:
    - "Generated report displays prominent header: AI-GENERATED DRAFT — NOT REVIEWED"
    - "Generated report displays footer with legal disclaimer"
    - "PDF export includes header and footer disclaimers"
    - "Word export includes header and footer disclaimers"
    - "Disclaimers are visually distinct from report content"
  artifacts:
    - path: "app/components/workspace/report-workspace.tsx"
      provides: "Report display with disclaimers, PDF/Word export with disclaimers"
      contains: "AI-GENERATED DRAFT"
  key_links:
    - from: "ReportTab render"
      to: "Disclaimer header/footer"
      via: "JSX elements in report display"
      pattern: "AI-GENERATED DRAFT"
    - from: "handleExportPDF"
      to: "PDF disclaimer content"
      via: "jsPDF text rendering"
    - from: "handleExportWord"
      to: "Word disclaimer content"
      via: "docx Header/Footer"
---

<objective>
Add prominent disclaimers to all generated reports in display view and exports.

Purpose: Legal compliance requirement OUTPUT-01, OUTPUT-02, OUTPUT-03 — ensure every generated report clearly shows it's an AI draft requiring review.
Output: Updated report-workspace.tsx with disclaimer header/footer in display and PDF/Word exports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Prior legal compliance phases completed:
- Phase 21: Terms/Privacy pages
- Phase 22: Sign-up checkboxes + acknowledgment modal
- Phase 23: Persistent amber banner
- Phase 24: Page-specific warnings (PageWarning component)

Source file:
@app/components/workspace/report-workspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disclaimer header/footer to report display</name>
  <files>app/components/workspace/report-workspace.tsx</files>
  <action>
In the ReportTab component, modify the report display section (currently lines 1218-1244) to add:

1. **Disclaimer Header** - Add BEFORE the report content (after line 1233):
   - Prominent amber/warning banner with icon
   - Text: "AI-GENERATED DRAFT — NOT REVIEWED"
   - Style: amber background, bold text, distinct from content
   - Full width, centered text

2. **Disclaimer Footer** - Add AFTER the report content (after the ReactMarkdown article):
   - Subtle but visible footer
   - Text: "Generated with AI assistance. User is solely responsible for accuracy. Not medical advice."
   - Style: gray border top, smaller text, slate color

Implementation notes:
- Use similar styling pattern to PageWarning but with amber/warning colors for header
- Footer should be less prominent but still visible
- Both should be inside the report container but visually separate from content
- Import AlertTriangle from lucide-react for the header icon
  </action>
  <verify>
1. Run `cd /Users/anand/rad-ai-auto/app && pnpm build` - should compile without errors
2. Visually: Report display shows header "AI-GENERATED DRAFT — NOT REVIEWED" above content
3. Visually: Report display shows footer disclaimer below content
  </verify>
  <done>
- Disclaimer header displays prominently above generated report content
- Disclaimer footer displays below generated report content
- Both visually distinct from report body
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PDF export disclaimers</name>
  <files>app/components/workspace/report-workspace.tsx</files>
  <action>
In handleExportPDF function (lines 190-434), update the disclaimer content:

1. **Add Disclaimer Header** - Add after metadata box, BEFORE content (around line 305):
   - Draw an amber/orange background rectangle
   - Text: "AI-GENERATED DRAFT — NOT REVIEWED"
   - Bold, centered, prominent
   - Add spacing after it before content starts

2. **Update Footer Text** - Modify the existing footer (lines 243-252):
   - Change footerText default to: "Generated with AI assistance. User is solely responsible for accuracy. Not medical advice."
   - Keep existing "AI-Generated Report | Exported: {timestamp}" line

Implementation:
```javascript
// After metadata box (around line 305), add disclaimer header:
y += 5;
// Disclaimer banner
pdf.setFillColor(254, 243, 199); // amber-100
pdf.roundedRect(margin, y, contentWidth, 12, 2, 2, 'F');
pdf.setFontSize(11);
pdf.setFont('helvetica', 'bold');
pdf.setTextColor(180, 83, 9); // amber-700
pdf.text('AI-GENERATED DRAFT — NOT REVIEWED', pageWidth / 2, y + 8, { align: 'center' });
y += 18;

// Update footer default text:
const footerText = brandTemplate?.footerText || 'Generated with AI assistance. User is solely responsible for accuracy. Not medical advice.';
```
  </action>
  <verify>
1. Build succeeds: `cd /Users/anand/rad-ai-auto/app && pnpm build`
2. Manually test: Generate a report, export to PDF, verify:
   - Amber header banner with "AI-GENERATED DRAFT — NOT REVIEWED" appears after metadata
   - Footer contains required disclaimer text
  </verify>
  <done>
- PDF export includes prominent disclaimer header after metadata
- PDF export footer contains required legal disclaimer text
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Word export disclaimers</name>
  <files>app/components/workspace/report-workspace.tsx</files>
  <action>
In handleExportWord function (lines 436-638), update the disclaimer content:

1. **Add Disclaimer Header** - Add after metadata section, before content paragraphs (around line 627):
   - Add a highlighted paragraph with shading
   - Text: "AI-GENERATED DRAFT — NOT REVIEWED"
   - Bold, centered, amber/orange shading

2. **Update Footer Text** - Modify the footer section (lines 573-596):
   - Change the footer text to: "Generated with AI assistance. User is solely responsible for accuracy. Not medical advice."
   - Keep the "AI-Generated Report" secondary line

Implementation:
```javascript
// Import Shading from docx (add to existing imports)
import { Shading } from 'docx';

// After metadata section, add disclaimer paragraph (before ...contentParagraphs):
new Paragraph({
  children: [
    new TextRun({
      text: 'AI-GENERATED DRAFT — NOT REVIEWED',
      bold: true,
      size: 24, // 12pt
      color: 'B45309', // amber-700
    }),
  ],
  alignment: AlignmentType.CENTER,
  shading: {
    type: ShadingType.CLEAR,
    fill: 'FEF3C7', // amber-100
  },
  spacing: { before: 200, after: 200 },
}),

// Update footer text default:
const footerText = brandTemplate?.footerText || 'Generated with AI assistance. User is solely responsible for accuracy. Not medical advice.';
```

Note: Check docx library for correct shading syntax. May need to use:
```javascript
shading: { fill: 'FEF3C7' }
```
  </action>
  <verify>
1. Build succeeds: `cd /Users/anand/rad-ai-auto/app && pnpm build`
2. Manually test: Generate a report, export to Word, verify:
   - Disclaimer header with amber background appears after metadata
   - Footer contains required legal disclaimer text
  </verify>
  <done>
- Word export includes prominent disclaimer header after metadata
- Word export footer contains required legal disclaimer text
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/anand/rad-ai-auto/app && pnpm build` succeeds without errors
- [ ] Report display shows disclaimer header "AI-GENERATED DRAFT — NOT REVIEWED"
- [ ] Report display shows disclaimer footer with required text
- [ ] PDF export includes disclaimer header and footer
- [ ] Word export includes disclaimer header and footer
- [ ] Disclaimers are visually distinct (amber header, slate footer)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No build errors or TypeScript errors
- OUTPUT-01: Report displays header disclaimer ✓
- OUTPUT-02: Report displays footer disclaimer ✓
- OUTPUT-03: PDF/DOCX exports include both disclaimers ✓
</success_criteria>

<output>
After completion, create `.planning/phases/25-report-disclaimers/25-01-SUMMARY.md`
</output>
