---
phase: 29-code-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/(protected)/generate/page.tsx
  - app/components/workspace/report-workspace.tsx
  - app/lib/export/pdf-generator.ts
  - app/lib/export/word-generator.ts
autonomous: true
must_haves:
  truths:
    - "docx library is dynamically imported only when user triggers Word export"
    - "jsPDF library is dynamically imported only when user triggers PDF export"
    - "Initial page load does not include export libraries in bundle"
  artifacts:
    - path: "app/lib/export/pdf-generator.ts"
      provides: "PDF export logic with dynamic jsPDF import"
      exports: ["generatePDF"]
    - path: "app/lib/export/word-generator.ts"
      provides: "Word export logic with dynamic docx import"
      exports: ["generateWord"]
  key_links:
    - from: "report-workspace.tsx"
      to: "lib/export/pdf-generator.ts"
      via: "dynamic import on export button click"
      pattern: "import\\(.*pdf-generator"
    - from: "generate/page.tsx"
      to: "lib/export/word-generator.ts"
      via: "dynamic import on export button click"
      pattern: "import\\(.*word-generator"
---

<objective>
Reduce initial bundle size by dynamically importing heavy export libraries.

Purpose: docx (~350KB) and jsPDF (~280KB) are only needed when exporting. Moving them to dynamic imports saves ~630KB from the initial bundle.
Output: Export utilities extracted to separate modules with dynamic imports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files
@app/components/workspace/report-workspace.tsx
@app/app/(protected)/generate/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Word export to dynamic module</name>
  <files>app/lib/export/word-generator.ts, app/app/(protected)/generate/page.tsx</files>
  <action>
    1. Create app/lib/export/word-generator.ts:
       - Move all Word/DOCX generation logic from generate/page.tsx
       - Keep the existing docx import at the top of THIS file (it will be code-split)
       - Export a single async function: generateWord(content, filename, options)
       - Include all the Document, Paragraph, TextRun, etc. logic
       - Handle the list style preferences formatting

    2. Update generate/page.tsx:
       - Remove the static `import { ... } from 'docx'`
       - Replace the inline Word export code with:
         ```typescript
         const handleWordExport = async () => {
           const { generateWord } = await import('@/lib/export/word-generator');
           await generateWord(reportContent, filename, { listStyles: preferences.listStyles });
         };
         ```
       - This ensures docx is only loaded when user clicks export

    Note: The docx import inside word-generator.ts will be automatically code-split by Next.js
    because the module is only dynamically imported.
  </action>
  <verify>
    1. npm run build succeeds
    2. Word export still works correctly
    3. Check bundle analyzer if available (ANALYZE=true npm run build)
  </verify>
  <done>Word export dynamically loaded, docx removed from initial bundle</done>
</task>

<task type="auto">
  <name>Task 2: Extract PDF export to dynamic module</name>
  <files>app/lib/export/pdf-generator.ts, app/components/workspace/report-workspace.tsx</files>
  <action>
    1. Create app/lib/export/pdf-generator.ts:
       - Move PDF generation logic from report-workspace.tsx
       - Keep the jsPDF import at the top of THIS file
       - Export: generatePDF(content, filename, options)
       - Include header/footer disclaimer handling
       - Include list style formatting

    2. Update report-workspace.tsx:
       - Remove static jsPDF import
       - Replace inline PDF code with dynamic import:
         ```typescript
         const handlePDFExport = async () => {
           const { generatePDF } = await import('@/lib/export/pdf-generator');
           await generatePDF(reportContent, filename, {
             header: disclaimerHeader,
             footer: disclaimerFooter,
             listStyles: listStylePreferences
           });
         };
         ```

    The module will be automatically code-split because it's dynamically imported.
  </action>
  <verify>
    1. npm run build succeeds
    2. PDF export still works correctly
    3. Disclaimer headers/footers appear in exported PDF
  </verify>
  <done>PDF export dynamically loaded, jsPDF removed from initial bundle</done>
</task>

<task type="auto">
  <name>Task 3: Verify bundle size reduction</name>
  <files>None (verification only)</files>
  <action>
    Run bundle analysis to verify the improvements:

    1. Run: ANALYZE=true npm run build
    2. Check the client bundle for docx and jsPDF
    3. These should now appear in separate chunks, not the main bundle

    Expected savings:
    - docx: ~350KB moved to dynamic chunk
    - jsPDF: ~280KB moved to dynamic chunk
    - Total: ~630KB reduction in initial load

    If ANALYZE env var isn't set up, just verify the build succeeds and note
    that proper verification requires bundle analyzer configuration.
  </action>
  <verify>
    1. npm run build succeeds
    2. No static imports of docx or jsPDF in main pages
    3. grep -r "from 'docx'" app/app should only show word-generator.ts
    4. grep -r "from 'jspdf'" app/components should only show pdf-generator.ts
  </verify>
  <done>Bundle size reduction verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm run typecheck` passes
- [ ] Word export works correctly
- [ ] PDF export works correctly
- [ ] Export libraries only in dynamic chunks (verify with grep)
- [ ] No static docx/jsPDF imports in page components
</verification>

<success_criteria>

- All tasks completed
- Export utilities extracted to lib/export/
- Dynamic imports used for both export functions
- Build passes
- Export functionality unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/29-code-refactoring/29-02-SUMMARY.md`
</output>
