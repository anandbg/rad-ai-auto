---
phase: 23-disclaimer-banner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/legal/disclaimer-banner.tsx
  - app/app/(protected)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Every authenticated page shows persistent warning banner"
    - "Banner cannot be dismissed (no close button)"
    - "Banner text warns about personal data and AI-generated content"
    - "Banner is visually prominent but not obstructive"
  artifacts:
    - path: "app/components/legal/disclaimer-banner.tsx"
      provides: "Persistent disclaimer banner component"
      min_lines: 15
    - path: "app/app/(protected)/layout.tsx"
      provides: "Layout with banner integration"
      contains: "DisclaimerBanner"
  key_links:
    - from: "app/app/(protected)/layout.tsx"
      to: "DisclaimerBanner"
      via: "React component import and render"
      pattern: "<DisclaimerBanner"
---

<objective>
Implement persistent app-wide disclaimer banner on all authenticated pages.

Purpose: Legal compliance - ensure every user sees warning about not uploading personal data and AI-generated content requiring review.
Output: Non-dismissible disclaimer banner visible on all protected routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sign-up-acknowledgment/22-02-SUMMARY.md

@app/app/(protected)/layout.tsx
@app/components/layout/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DisclaimerBanner component</name>
  <files>app/components/legal/disclaimer-banner.tsx</files>
  <action>
Create a slim, non-dismissible disclaimer banner component:

1. **Create `app/components/legal/disclaimer-banner.tsx`:**
   - Export `DisclaimerBanner` functional component
   - Use amber/yellow background for visual prominence (bg-amber-50 with amber-600 text and border)
   - Include warning icon (AlertTriangle from lucide-react, matches existing legal components)
   - Display text: "⚠️ Do not upload personal data. AI-generated content requires review. Use at your own risk."
   - NO close button or dismiss functionality
   - Slim height (~40px) - use py-2 for minimal padding
   - Full width, fixed at top position
   - z-index above content but below modals (z-40)
   - Responsive text sizing (text-sm on mobile, text-sm on desktop)
   - Center the warning text with icon

2. **Styling requirements:**
   - Amber warning color scheme: `bg-amber-50 border-b border-amber-200`
   - Text color: `text-amber-800`
   - Icon: `text-amber-600`
   - Use flex layout centered with gap-2 for icon spacing
   - Add subtle shadow: `shadow-sm`
  </action>
  <verify>
    - Component exists at app/components/legal/disclaimer-banner.tsx
    - npm run build succeeds
    - No TypeScript errors
  </verify>
  <done>
    - DisclaimerBanner component created with amber styling
    - No dismiss functionality (no close button)
    - Warning icon included
    - Correct text displayed
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate banner into protected layout</name>
  <files>app/app/(protected)/layout.tsx</files>
  <action>
Integrate the DisclaimerBanner into the protected layout so it appears on all authenticated pages:

1. **Import the component:**
   ```typescript
   import { DisclaimerBanner } from '@/components/legal/disclaimer-banner';
   ```

2. **Render banner ABOVE AppShell:**
   - Place banner before AppShell in the render tree
   - This ensures it appears at the very top of the page
   - Banner should be outside the scrollable content area

3. **Update layout structure:**
   ```tsx
   return (
     <>
       <DisclaimerBanner />
       <AppShell>
         {children}
       </AppShell>
       {/* ... existing CommandPalette and Modal ... */}
     </>
   );
   ```

4. **Adjust AppShell for banner offset:**
   - The banner is fixed position, so main content needs top offset
   - Update AppShell's main content area to account for banner height (~40px)
   - Add `pt-10` or similar to main content wrapper OR use `top-10` on AppShell's fixed elements

5. **Alternative approach (simpler):**
   - If AppShell structure doesn't need changes, wrap everything in a flex column:
   ```tsx
   <div className="flex flex-col h-screen">
     <DisclaimerBanner />
     <div className="flex-1 overflow-hidden">
       <AppShell>{children}</AppShell>
     </div>
   </div>
   ```
  </action>
  <verify>
    - Banner appears at top of all authenticated pages
    - Banner persists through navigation
    - Main content is not obscured by banner
    - npm run build succeeds
  </verify>
  <done>
    - DisclaimerBanner rendered in protected layout
    - Banner visible on all authenticated routes
    - Content properly offset below banner
    - No layout conflicts with sidebar or modals
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Banner visible on /dashboard
- [ ] Banner visible on /templates
- [ ] Banner visible on /settings
- [ ] Banner has amber/yellow styling
- [ ] Banner has warning icon
- [ ] Banner displays correct text
- [ ] Banner has NO close button
- [ ] Main content scrolls independently of banner
- [ ] Banner stays fixed at top during scroll
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- BANNER-01 requirement satisfied: Persistent banner on all authenticated pages
- BANNER-02 requirement satisfied: Visually prominent but slim banner
</success_criteria>

<output>
After completion, create `.planning/phases/23-disclaimer-banner/23-01-SUMMARY.md`
</output>
