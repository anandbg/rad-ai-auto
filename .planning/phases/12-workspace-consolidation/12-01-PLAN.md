---
phase: 12-workspace-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/layout/app-shell.tsx
  - app/components/layout/sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows 2-panel layout: sidebar + workspace (no reports panel)"
    - "Sidebar has no toggle button for reports panel"
    - "Sidebar navigation is simplified (Transcribe, Generate pages removed as redundant)"
  artifacts:
    - path: "app/components/layout/app-shell.tsx"
      provides: "2-panel layout without ReportsPanel"
      contains: "flex h-screen"
    - path: "app/components/layout/sidebar.tsx"
      provides: "Simplified navigation without panel controls"
  key_links:
    - from: "AppShell"
      to: "Sidebar + main content"
      via: "flex layout with 2 children only"
      pattern: "Sidebar.*children"
---

<objective>
Remove the reports panel from the dashboard and simplify to a 2-panel layout.

Purpose: Consolidate the UI to a cleaner sidebar + workspace layout, removing the middle reports panel that adds complexity without value at this stage.
Output: Simplified AppShell with 2-panel layout, cleaner sidebar navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 11 established the 3-panel shell with reports panel
@.planning/phases/11-ui-ux-overhaul/11-07-SUMMARY.md

# Current implementation:
@app/components/layout/app-shell.tsx
@app/components/layout/sidebar.tsx
@app/components/layout/reports-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify AppShell to 2-panel layout</name>
  <files>app/components/layout/app-shell.tsx</files>
  <action>
    Remove the ReportsPanel component and its toggle logic entirely:
    1. Remove import for ReportsPanel
    2. Remove `reportsPanelVisible` state and `showReportsPanel` prop
    3. Remove the AnimatePresence block containing ReportsPanel
    4. Remove `onToggleReportsPanel` and `reportsPanelVisible` props passed to Sidebar
    5. Keep the Sidebar and main content structure (just 2 children in the flex container)

    Result: Simple 2-panel layout - Sidebar on left, main content fills remaining space.
  </action>
  <verify>TypeScript compiles without errors: `cd app && npx tsc --noEmit`</verify>
  <done>AppShell renders with only Sidebar + main content (no ReportsPanel)</done>
</task>

<task type="auto">
  <name>Task 2: Clean up Sidebar panel controls and navigation</name>
  <files>app/components/layout/sidebar.tsx</files>
  <action>
    1. Remove props related to reports panel:
       - Remove `onToggleReportsPanel` from SidebarProps
       - Remove `reportsPanelVisible` from SidebarProps
       - Remove the reports panel toggle button (LayoutList icon button)

    2. Simplify navigation by removing redundant pages:
       - Remove Transcribe from navItems (redundant - workspace has Voice Input tab)
       - Remove Generate from navItems (redundant - workspace has Report tab + Generate button)

    3. Keep these navigation items:
       - Dashboard (main workspace hub)
       - Templates (template management)
       - Branding (brand templates)
       - Macros (text shortcuts)
       - Productivity (usage stats)
       - Billing (subscription)
       - Settings (preferences)
       - Admin (admin only)

    Result: Cleaner sidebar with 8 navigation items instead of 10.
  </action>
  <verify>TypeScript compiles: `cd app && npx tsc --noEmit`</verify>
  <done>Sidebar has no panel controls, navigation has 8 items (no Transcribe/Generate)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npx tsc --noEmit` passes
- [ ] `cd app && npm run build` succeeds
- [ ] Dashboard loads with 2-panel layout (visually confirmed)
- [ ] Sidebar shows 8 nav items (Dashboard, Templates, Branding, Macros, Productivity, Billing, Settings, Admin)
</verification>

<success_criteria>
- All tasks completed
- TypeScript compiles without errors
- Dashboard shows 2-panel layout (sidebar + workspace)
- No reports panel visible or toggleable
- Sidebar navigation simplified to 8 items
</success_criteria>

<output>
After completion, create `.planning/phases/12-workspace-consolidation/12-01-SUMMARY.md`
</output>
