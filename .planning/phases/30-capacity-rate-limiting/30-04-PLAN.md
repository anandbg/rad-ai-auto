---
phase: 30-capacity-rate-limiting
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - app/app/api/generate/route.ts
autonomous: true
must_haves:
  truths:
    - "Generate endpoint checks rate limit before OpenAI call"
    - "Generate endpoint checks monthly usage limit before OpenAI call"
    - "OpenAI call is wrapped in retry logic"
    - "Usage is recorded after successful generation"
    - "Rate limit response includes Retry-After header"
  artifacts:
    - path: "app/app/api/generate/route.ts"
      provides: "Report generation API with rate limiting"
      contains: "checkRateLimit"
  key_links:
    - from: "app/app/api/generate/route.ts"
      to: "app/lib/ratelimit/limiters.ts"
      via: "imports checkRateLimit"
      pattern: "import.*checkRateLimit.*from.*ratelimit"
    - from: "app/app/api/generate/route.ts"
      to: "app/lib/openai/retry.ts"
      via: "imports withStreamRetry"
      pattern: "import.*withStreamRetry.*from.*openai"
---

<objective>
Integrate rate limiting, retry logic, and usage tracking into the report generation endpoint.

Purpose: Protect the most expensive OpenAI endpoint (GPT-4o) from abuse and ensure fair usage.
Output: Generate endpoint with full rate limiting and usage tracking.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-capacity-rate-limiting/30-RESEARCH.md

# Current implementation to modify
@app/app/api/generate/route.ts

# New utilities from Wave 1
# @app/lib/ratelimit/limiters.ts (created in 30-01)
# @app/lib/openai/retry.ts (created in 30-02)
# @app/lib/openai/errors.ts (created in 30-02)
# @app/lib/usage/limits.ts (created in 30-03)
# @app/lib/usage/tracker.ts (created in 30-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiting and retry to generate endpoint</name>
  <files>app/app/api/generate/route.ts</files>
  <action>
    Modify the existing generate route to add:
    1. Per-user rate limiting (check before OpenAI call)
    2. Monthly usage limit check
    3. Retry wrapper for OpenAI calls
    4. Usage recording after success

    Add these imports at the top:
    ```typescript
    import { checkRateLimit } from "@/lib/ratelimit/limiters";
    import { checkMonthlyUsage, formatUsageHeaders } from "@/lib/usage/limits";
    import { recordUsage } from "@/lib/usage/tracker";
    import { withStreamRetry } from "@/lib/openai/retry";
    import { formatErrorResponse } from "@/lib/openai/errors";
    ```

    After authentication succeeds (after line ~97 where we have `const user`), add rate limiting checks:

    ```typescript
    // === RATE LIMITING: Check per-minute rate limit ===
    // Get user's subscription plan for rate limiting
    const { data: subscription } = await supabase
      .from("subscriptions")
      .select("plan, status")
      .eq("user_id", user.id)
      .single();

    const isActiveSubscription = subscription?.status === "active" || subscription?.status === "trialing";
    const userPlan = (isActiveSubscription && subscription?.plan) || "free";

    const rateLimitResult = await checkRateLimit(user.id, userPlan, "generate");

    if (!rateLimitResult.success) {
      const retryAfterSeconds = Math.ceil((rateLimitResult.reset - Date.now()) / 1000);
      return new Response(
        JSON.stringify({
          success: false,
          error: "Rate Limit Exceeded",
          message: `Too many requests. Please wait ${retryAfterSeconds} seconds before trying again.`,
          retryAfter: retryAfterSeconds,
        }),
        {
          status: 429,
          headers: {
            "Content-Type": "application/json",
            "Retry-After": String(retryAfterSeconds),
            "X-RateLimit-Remaining": String(rateLimitResult.remaining),
            "X-RateLimit-Limit": String(rateLimitResult.limit),
          },
        }
      );
    }

    // === USAGE LIMITING: Check monthly usage limit ===
    const usageResult = await checkMonthlyUsage(user.id, "reports");

    if (!usageResult.allowed) {
      return new Response(
        JSON.stringify({
          success: false,
          error: "Monthly Limit Reached",
          message: `You've used ${usageResult.currentUsage} of ${usageResult.limit} reports this month. ` +
            `Your limit resets on ${usageResult.resetDate.toLocaleDateString()}.`,
          currentUsage: usageResult.currentUsage,
          limit: usageResult.limit,
          resetDate: usageResult.resetDate.toISOString(),
        }),
        {
          status: 429,
          headers: {
            "Content-Type": "application/json",
            ...formatUsageHeaders(usageResult),
          },
        }
      );
    }
    ```

    Replace the streamText call (around line ~285) with retry wrapper:
    ```typescript
    // Generate report using GPT-4o with streaming and retry
    try {
      const result = await withStreamRetry(
        async () => streamText({
          model: openai('gpt-4o'),
          system: systemPrompt,
          prompt: userPrompt,
          temperature: 0.2,
          maxOutputTokens: 2000,
        }),
        { operationName: "generate-report" }
      );

      // Record successful usage (non-blocking)
      recordUsage(user.id, "report", {
        templateId: validation.data.templateId,
        modality: validation.data.modality,
      }).catch((err) => console.error("[Usage] Failed to record:", err));

      // Return streaming response with rate limit info
      const response = result.toTextStreamResponse();

      // Add rate limit headers to successful response
      const headers = new Headers(response.headers);
      headers.set("X-RateLimit-Remaining", String(rateLimitResult.remaining - 1));
      headers.set("X-RateLimit-Limit", String(rateLimitResult.limit));

      return new Response(response.body, {
        status: response.status,
        headers,
      });
    } catch (error) {
      // Handle OpenAI errors after retries exhausted
      console.error("[Generate] OpenAI error after retries:", error);
      const errorResponse = formatErrorResponse(error);
      return new Response(
        JSON.stringify({
          success: false,
          error: errorResponse.error,
          message: errorResponse.message,
        }),
        { status: errorResponse.statusCode, headers: { "Content-Type": "application/json" } }
      );
    }
    ```

    Key changes:
    1. Import rate limiting and usage utilities
    2. Check rate limit BEFORE validation (fail fast)
    3. Check monthly usage limit
    4. Wrap OpenAI call in retry logic
    5. Record usage on success (non-blocking)
    6. Add rate limit headers to response
    7. Better error handling for OpenAI failures
  </action>
  <verify>
    - TypeScript compiles: cd app && npx tsc --noEmit
    - Build succeeds: cd app && npm run build
  </verify>
  <done>Generate endpoint has rate limiting, usage limits, retry logic, and usage tracking</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles: `cd app && npx tsc --noEmit`
- [ ] Build succeeds: `cd app && npm run build`
- [ ] Route includes rate limit check
- [ ] Route includes usage limit check
- [ ] Route includes retry wrapper
- [ ] Route records usage on success
</verification>

<success_criteria>
- All tasks completed
- Generate endpoint protected by rate limiting
- Monthly limits enforced
- Retry logic handles transient failures
- Usage recorded for analytics
</success_criteria>

<output>
After completion, create `.planning/phases/30-capacity-rate-limiting/30-04-SUMMARY.md`
</output>
