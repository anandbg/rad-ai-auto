---
phase: 30-capacity-rate-limiting
plan: 06
type: execute
wave: 2
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - app/app/api/templates/generate/route.ts
autonomous: true
must_haves:
  truths:
    - "Template generate endpoint checks rate limit before OpenAI call"
    - "Template generate endpoint checks monthly usage limit before OpenAI call"
    - "OpenAI call is wrapped in retry logic"
    - "Usage is recorded after successful generation"
    - "Rate limit response includes Retry-After header"
  artifacts:
    - path: "app/app/api/templates/generate/route.ts"
      provides: "AI template generation API with rate limiting"
      contains: "checkRateLimit"
  key_links:
    - from: "app/app/api/templates/generate/route.ts"
      to: "app/lib/ratelimit/limiters.ts"
      via: "imports checkRateLimit"
      pattern: "import.*checkRateLimit.*from.*ratelimit"
    - from: "app/app/api/templates/generate/route.ts"
      to: "app/lib/openai/retry.ts"
      via: "imports withRetry"
      pattern: "import.*withRetry.*from.*openai"
---

<objective>
Integrate rate limiting, retry logic, and usage tracking into the template generation endpoint.

Purpose: Protect the AI template generation endpoint from abuse and track template creation usage.
Output: Template generate endpoint with full rate limiting and usage tracking.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-capacity-rate-limiting/30-RESEARCH.md

# Current implementation to modify
@app/app/api/templates/generate/route.ts

# New utilities from Wave 1
# @app/lib/ratelimit/limiters.ts (created in 30-01)
# @app/lib/openai/retry.ts (created in 30-02)
# @app/lib/usage/limits.ts (created in 30-03)
# @app/lib/usage/tracker.ts (created in 30-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiting and retry to template generate endpoint</name>
  <files>app/app/api/templates/generate/route.ts</files>
  <action>
    Modify the existing template generate route to add:
    1. Per-user rate limiting
    2. Monthly usage limit check (using "reports" type since templates are similar)
    3. Retry wrapper for OpenAI generateText calls
    4. Usage recording after success

    Add these imports at the top (after existing imports):
    ```typescript
    import { checkRateLimit } from "@/lib/ratelimit/limiters";
    import { checkMonthlyUsage, formatUsageHeaders } from "@/lib/usage/limits";
    import { recordUsage } from "@/lib/usage/tracker";
    import { withRetry } from "@/lib/openai/retry";
    import { formatErrorResponse } from "@/lib/openai/errors";
    ```

    After authentication succeeds (after line ~118 where we have `const user`), add rate limiting:

    ```typescript
    // === RATE LIMITING: Check per-minute rate limit ===
    const supabaseForSub = await createEdgeSupabaseClient();
    const { data: subscription } = await supabaseForSub
      .from("subscriptions")
      .select("plan, status")
      .eq("user_id", user.id)
      .single();

    const isActiveSubscription = subscription?.status === "active" || subscription?.status === "trialing";
    const userPlan = (isActiveSubscription && subscription?.plan) || "free";

    const rateLimitResult = await checkRateLimit(user.id, userPlan, "template");

    if (!rateLimitResult.success) {
      const retryAfterSeconds = Math.ceil((rateLimitResult.reset - Date.now()) / 1000);
      return new Response(
        JSON.stringify({
          success: false,
          error: "Rate Limit Exceeded",
          message: `Too many requests. Please wait ${retryAfterSeconds} seconds before trying again.`,
          retryAfter: retryAfterSeconds,
        }),
        {
          status: 429,
          headers: {
            "Content-Type": "application/json",
            "Retry-After": String(retryAfterSeconds),
            "X-RateLimit-Remaining": String(rateLimitResult.remaining),
            "X-RateLimit-Limit": String(rateLimitResult.limit),
          },
        }
      );
    }

    // === USAGE LIMITING: Check monthly usage limit ===
    // Note: Template generation counts towards report limits
    const usageResult = await checkMonthlyUsage(user.id, "reports");

    if (!usageResult.allowed) {
      return new Response(
        JSON.stringify({
          success: false,
          error: "Monthly Limit Reached",
          message: `You've used ${usageResult.currentUsage} of ${usageResult.limit} AI generations this month. ` +
            `Your limit resets on ${usageResult.resetDate.toLocaleDateString()}.`,
          currentUsage: usageResult.currentUsage,
          limit: usageResult.limit,
          resetDate: usageResult.resetDate.toISOString(),
        }),
        {
          status: 429,
          headers: {
            "Content-Type": "application/json",
            ...formatUsageHeaders(usageResult),
          },
        }
      );
    }
    ```

    Replace the generateText call (around line ~227) with retry wrapper:
    ```typescript
    // Generate template using GPT-4o with structured output and retry
    try {
      const result = await withRetry(
        async () => generateText({
          model: openai('gpt-4o'),
          system: systemPrompt,
          prompt: userPrompt,
          output: Output.object({ schema: aiGeneratedTemplateSchema }),
          temperature: 0.3,
        }),
        { maxRetries: 3, operationName: "template-generate" }
      );

      // Extract the validated output
      const output = result.output;

      // Record successful usage (non-blocking)
      recordUsage(user.id, "template", {
        mode,
        modality: modality || output?.modality,
        bodyPart: bodyPart || output?.bodyPart,
      }).catch((err) => console.error("[Usage] Failed to record:", err));

      // Return the validated template with rate limit info
      return new Response(
        JSON.stringify({
          success: true,
          data: output,
        }),
        {
          status: 200,
          headers: {
            "Content-Type": "application/json",
            "X-RateLimit-Remaining": String(rateLimitResult.remaining - 1),
            "X-RateLimit-Limit": String(rateLimitResult.limit),
          },
        }
      );
    } catch (error) {
      console.error("[Template Generate] OpenAI error after retries:", error);
      const errorResponse = formatErrorResponse(error);
      return new Response(
        JSON.stringify({
          success: false,
          error: errorResponse.error,
          message: errorResponse.message,
        }),
        { status: errorResponse.statusCode, headers: { "Content-Type": "application/json" } }
      );
    }
    ```

    Key changes:
    1. Import rate limiting and usage utilities
    2. Check rate limit after auth
    3. Check monthly usage limit
    4. Wrap OpenAI call in retry logic
    5. Record usage on success
    6. Add rate limit headers to response
    7. Better error handling
  </action>
  <verify>
    - TypeScript compiles: cd app && npx tsc --noEmit
    - Build succeeds: cd app && npm run build
  </verify>
  <done>Template generate endpoint has rate limiting, usage limits, retry logic, and usage tracking</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles: `cd app && npx tsc --noEmit`
- [ ] Build succeeds: `cd app && npm run build`
- [ ] Route includes rate limit check
- [ ] Route includes usage limit check
- [ ] Route includes retry wrapper
- [ ] Route records usage on success
</verification>

<success_criteria>
- All tasks completed
- Template generate endpoint protected by rate limiting
- Monthly limits enforced
- Retry logic handles transient failures
- Usage recorded for analytics
</success_criteria>

<output>
After completion, create `.planning/phases/30-capacity-rate-limiting/30-06-SUMMARY.md`
</output>
