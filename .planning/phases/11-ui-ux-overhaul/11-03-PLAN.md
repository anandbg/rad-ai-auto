---
phase: 11-ui-ux-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - app/app/login/page.tsx
  - app/app/signup/page.tsx
  - app/app/verify-email/page.tsx
  - app/app/forgot-password/page.tsx
  - app/app/reset-password/page.tsx
autonomous: false

must_haves:
  truths:
    - "Auth pages have consistent visual styling with refined cards"
    - "Form elements fade in with staggered animation on page load"
    - "Success/error messages animate smoothly"
    - "All auth pages share consistent layout and branding"
  artifacts:
    - path: "app/app/login/page.tsx"
      provides: "Polished login page with motion"
      contains: "FadeIn\\|PageWrapper"
    - path: "app/app/signup/page.tsx"
      provides: "Polished signup page with motion"
      contains: "FadeIn\\|PageWrapper"
  key_links:
    - from: "app/app/login/page.tsx"
      to: "app/components/motion/"
      via: "imports motion components"
      pattern: "import.*from.*@/components/motion"
---

<objective>
Apply visual polish and motion to all authentication pages (login, signup, verify-email, forgot-password, reset-password).

Purpose: First impression matters. Auth pages are the first thing users see. Consistent, polished auth flows establish trust and quality perception.

Output: All 5 auth pages with consistent styling, refined cards, staggered form animations, and proper loading states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-ux-overhaul/11-RESEARCH.md

# Motion system from Plan 01:
@app/lib/motion/constants.ts
@app/lib/motion/variants.ts
@app/components/motion/fade-in.tsx
@app/components/motion/page-wrapper.tsx
@app/components/motion/stagger-container.tsx

# Enhanced components from Plan 02:
@app/components/ui/button.tsx
@app/components/ui/card.tsx
@app/components/ui/input.tsx

# Auth pages to polish:
@app/app/login/page.tsx
@app/app/signup/page.tsx
@app/app/verify-email/page.tsx
@app/app/forgot-password/page.tsx
@app/app/reset-password/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish login page</name>
  <files>app/app/login/page.tsx</files>
  <action>
Read the existing login page first. Apply visual polish:

1. Wrap page content with PageWrapper for fade-in on load
2. Use Card component for the login form container (if not already)
3. Wrap form fields in StaggerContainer with FadeIn children for sequential animation
4. Ensure consistent spacing (py-12 or similar for vertical centering)
5. Add subtle branding: small logo/icon + "AI Radiologist" text above form
6. Polish error messages: animate in with FadeIn
7. Ensure loading state uses the Spinner component

Layout structure:
```tsx
<PageWrapper className="min-h-screen flex items-center justify-center bg-background p-4">
  <Card className="w-full max-w-md p-8">
    <FadeIn>
      <div className="text-center mb-8">
        <span className="text-4xl mb-4 block">🩻</span>
        <h1 className="text-2xl font-bold">Welcome back</h1>
        <p className="text-text-secondary mt-1">Sign in to your account</p>
      </div>
    </FadeIn>
    <StaggerContainer>
      <FadeIn>{/* email field */}</FadeIn>
      <FadeIn>{/* password field */}</FadeIn>
      <FadeIn>{/* submit button */}</FadeIn>
      <FadeIn>{/* links */}</FadeIn>
    </StaggerContainer>
  </Card>
</PageWrapper>
```

IMPORTANT: Preserve ALL existing functionality (form submission, error handling, redirects).
IMPORTANT: Do not change the auth logic or API calls.
  </action>
  <verify>
Build succeeds: `cd app && npm run build`
Visual: Login form animates in sequentially, looks polished
  </verify>
  <done>Login page has polished visuals and motion</done>
</task>

<task type="auto">
  <name>Task 2: Polish remaining auth pages</name>
  <files>app/app/signup/page.tsx, app/app/verify-email/page.tsx, app/app/forgot-password/page.tsx, app/app/reset-password/page.tsx</files>
  <action>
Apply the same pattern from login to all other auth pages.

For each page:
1. Wrap with PageWrapper
2. Use Card for form container
3. Use StaggerContainer + FadeIn for form fields
4. Consistent heading/icon treatment
5. Polish error/success messages
6. Maintain existing functionality

**signup/page.tsx:**
- Title: "Create account"
- Fields: name, email, password, confirm password
- Link to login

**verify-email/page.tsx:**
- Title: "Check your email"
- Icon suggestion: ✉️ or 📧
- Status message for verification state
- Resend link

**forgot-password/page.tsx:**
- Title: "Reset password"
- Single email field
- Link back to login

**reset-password/page.tsx:**
- Title: "Set new password"
- Password + confirm password fields
- Success state with redirect

IMPORTANT: Each page already works - only add visual/motion enhancements.
IMPORTANT: Test that forms still submit correctly after changes.
  </action>
  <verify>
Build succeeds: `cd app && npm run build`
All 4 pages load without errors
  </verify>
  <done>All auth pages have consistent polished styling and motion</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Polished all 5 auth pages with consistent visual design and micro-interactions</what-built>
  <how-to-verify>
    1. Run: `cd app && npm run dev`
    2. Visit: http://localhost:3000/login
       - Page should fade in smoothly
       - Form fields should stagger in sequentially
       - Card should have subtle hover effect
       - Submit button should have scale animation on hover/click
    3. Visit: http://localhost:3000/signup
       - Same animation pattern as login
       - Consistent card and heading style
    4. Visit: http://localhost:3000/forgot-password
       - Consistent styling
    5. Try logging in with invalid credentials:
       - Error message should animate in smoothly
    6. Check dark mode (if toggle available):
       - Colors should remain readable, no contrast issues
    7. Resize browser to mobile width (~375px):
       - Cards should remain readable, not overflow
  </how-to-verify>
  <resume-signal>Type "approved" if auth pages look polished and consistent, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm run build` succeeds
- [ ] All 5 auth pages render without errors
- [ ] Motion components imported from correct paths
- [ ] Form submissions still work correctly
- [ ] Human verification passed
</verification>

<success_criteria>

- All tasks completed
- Human approved visual design
- No functional regressions
- Consistent visual language across all auth pages
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-ux-overhaul/11-03-SUMMARY.md`
</output>
