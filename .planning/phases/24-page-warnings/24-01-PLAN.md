---
phase: 24-page-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/legal/page-warning.tsx
  - app/components/workspace/report-workspace.tsx
  - app/app/(protected)/templates/new/page.tsx
  - app/app/(protected)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows reminder card about drafting tool and no personal information"
    - "Voice/transcription area shows warning about AI processing before recording"
    - "Report generation area shows warning about AI-generated draft requiring review"
    - "Template creation shows note about no personal information in templates"
  artifacts:
    - path: "app/components/legal/page-warning.tsx"
      provides: "Reusable warning card component with variants"
      min_lines: 20
    - path: "app/components/workspace/report-workspace.tsx"
      provides: "Voice and report warnings integrated"
      contains: "PageWarning"
    - path: "app/app/(protected)/templates/new/page.tsx"
      provides: "Template creation warning"
      contains: "PageWarning"
    - path: "app/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard reminder card"
      contains: "PageWarning"
  key_links:
    - from: "report-workspace.tsx"
      to: "page-warning.tsx"
      via: "import and render in transcribe/report tabs"
      pattern: "PageWarning.*variant"
    - from: "templates/new/page.tsx"
      to: "page-warning.tsx"
      via: "import and render above form"
      pattern: "PageWarning"
---

<objective>
Add context-specific warning components to key pages throughout the authenticated app.

Purpose: Ensure users see relevant privacy and AI-usage warnings at each touchpoint where they interact with sensitive functionality (voice input, report generation, template creation).

Output: Reusable PageWarning component with 4 variants integrated into Dashboard, ReportWorkspace, and Template Creation pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-disclaimer-banner/23-01-SUMMARY.md

# Existing legal component pattern:
@app/components/legal/disclaimer-banner.tsx

# Target files to modify:
@app/components/workspace/report-workspace.tsx
@app/app/(protected)/templates/new/page.tsx
@app/app/(protected)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable PageWarning component with variants</name>
  <files>app/components/legal/page-warning.tsx</files>
  <action>
Create a reusable warning card component at `app/components/legal/page-warning.tsx` with the following variants:

**Component structure:**
- Client component ('use client')
- Import Info icon from lucide-react
- Accept `variant` prop: 'dashboard' | 'transcription' | 'report' | 'template'
- Use blue/info styling (bg-blue-50, border-blue-200, text-blue-800) to distinguish from amber app-wide banner
- Non-dismissible (no close button)
- Accessible with role="note" for informational content

**Variant text:**
- `dashboard`: "This is a drafting tool. Do not enter patient-identifiable or personal information."
- `transcription`: "Audio is processed by AI and not stored. Do not dictate personal identifiers."
- `report`: "AI-generated draft. Review and verify all content before use."
- `template`: "Templates should not contain personal information."

**Styling:**
- Rounded corners (rounded-lg)
- Padding (p-4)
- Flex layout with icon and text
- Small text (text-sm)
- Match existing design system patterns from disclaimer-banner.tsx

**Export:** Named export `PageWarning`
  </action>
  <verify>TypeScript compiles: `cd app && npx tsc --noEmit`</verify>
  <done>PageWarning component exists with all 4 variants properly typed and styled</done>
</task>

<task type="auto">
  <name>Task 2: Add warning to Dashboard page</name>
  <files>app/app/(protected)/dashboard/page.tsx</files>
  <action>
Add the dashboard warning to the Dashboard page:

1. Import PageWarning: `import { PageWarning } from '@/components/legal/page-warning';`

2. Add the warning inside the PageWrapper, BEFORE the ReportWorkspace component:
   ```tsx
   <PageWrapper className="h-full">
     {showUnauthorizedError && (
       ...existing error alert...
     )}
     <div className="p-4 pt-2 pb-0">
       <PageWarning variant="dashboard" />
     </div>
     <ReportWorkspace />
   </PageWrapper>
   ```

The warning should appear as a card at the top of the dashboard content area (below any error alerts).
  </action>
  <verify>TypeScript compiles: `cd app && npx tsc --noEmit`</verify>
  <done>Dashboard shows "drafting tool" reminder card at top of content area</done>
</task>

<task type="auto">
  <name>Task 3: Add warnings to ReportWorkspace (transcription and report areas)</name>
  <files>app/components/workspace/report-workspace.tsx</files>
  <action>
Add context-specific warnings to the ReportWorkspace component:

1. Import PageWarning: `import { PageWarning } from '@/components/legal/page-warning';`

2. Find the transcription tab content (where the mic button and text input are) and add the warning at the TOP of that section:
   - Look for the section that renders when `activeTab === 'transcribe'`
   - Add `<PageWarning variant="transcription" />` with `className="mb-4"` before the transcription input area

3. Find the report tab content (where generated reports display) and add the warning at the TOP:
   - Look for the section that renders when `activeTab === 'report'`
   - Add `<PageWarning variant="report" />` with `className="mb-4"` before the report content display

**Important:** Place warnings INSIDE each tab's content area, not in shared/header sections. Users should see the relevant warning when they switch to each tab.

The component uses WorkspaceTabs with 'transcribe' and 'report' tabs - find the conditional rendering for each.
  </action>
  <verify>TypeScript compiles: `cd app && npx tsc --noEmit`</verify>
  <done>Transcription and report tabs each show their specific warning at top of content area</done>
</task>

<task type="auto">
  <name>Task 4: Add warning to Template Creation page</name>
  <files>app/app/(protected)/templates/new/page.tsx</files>
  <action>
Add the template creation warning to the new template page:

1. Import PageWarning: `import { PageWarning } from '@/components/legal/page-warning';`

2. Add the warning inside the left panel's form area, AFTER the "Draft Restored" notice (if present) and BEFORE the form:
   - Find the section inside `<Panel defaultSize={60}>` that contains the form
   - Add `<PageWarning variant="template" />` with `className="mb-4"` BEFORE the `<form>` element but AFTER the draft-restored notice

The warning should be visible immediately when users start creating a template, reminding them not to include personal information.
  </action>
  <verify>TypeScript compiles: `cd app && npx tsc --noEmit`</verify>
  <done>Template creation page shows "no personal information" warning above the form</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm run build` succeeds without errors
- [ ] PageWarning component exists in app/components/legal/
- [ ] Dashboard page imports and renders PageWarning with variant="dashboard"
- [ ] ReportWorkspace renders PageWarning with variant="transcription" in transcribe tab
- [ ] ReportWorkspace renders PageWarning with variant="report" in report tab
- [ ] Template new page imports and renders PageWarning with variant="template"
- [ ] All warnings use blue/info styling (distinct from amber app-wide banner)
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 4 page-specific warnings visible at appropriate touchpoints:
  1. Dashboard: "This is a drafting tool..."
  2. Transcription: "Audio is processed by AI..."
  3. Report: "AI-generated draft..."
  4. Template creation: "Templates should not contain..."
- Warnings are visually distinct from app-wide amber banner (blue/info style)
</success_criteria>

<output>
After completion, create `.planning/phases/24-page-warnings/24-01-SUMMARY.md`
</output>
