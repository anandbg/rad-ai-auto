---
phase: 22-sign-up-acknowledgment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/app/signup/page.tsx
  - app/lib/validation/signup-schema.ts
autonomous: true

must_haves:
  truths:
    - "User cannot submit signup form without checking ToS checkbox"
    - "User cannot submit signup form without checking no-PHI checkbox"
    - "Unchecked checkboxes show validation errors"
    - "Checkboxes link to Terms of Service page"
  artifacts:
    - path: "app/app/signup/page.tsx"
      provides: "Signup form with mandatory legal checkboxes"
      contains: "acceptTerms"
    - path: "app/lib/validation/signup-schema.ts"
      provides: "Zod schema for signup validation including checkbox fields"
      exports: ["signupSchema"]
  key_links:
    - from: "app/app/signup/page.tsx"
      to: "/terms"
      via: "Link component in checkbox label"
      pattern: "href.*terms"
---

<objective>
Add mandatory legal consent checkboxes to sign-up form

Purpose: Ensure users explicitly accept Terms of Service and acknowledge no-PHI policy before account creation (GATE-01, GATE-02)
Output: Updated signup page with two required checkboxes that block form submission
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-legal-documents/21-01-SUMMARY.md

# Source files:
@app/app/signup/page.tsx
@app/components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract signup schema to validation file</name>
  <files>app/lib/validation/signup-schema.ts</files>
  <action>
    Create a new validation file that extracts and extends the signup form schema.

    The schema should include:
    - All existing fields (name, email, password, confirmPassword)
    - New field: `acceptTerms` - boolean, must be true (z.literal(true) with custom error)
    - New field: `acceptNoPhiPolicy` - boolean, must be true (z.literal(true) with custom error)

    Error messages:
    - acceptTerms: "You must accept the Terms of Service to continue"
    - acceptNoPhiPolicy: "You must acknowledge the no personal data policy to continue"

    Export both the schema and the inferred type.
  </action>
  <verify>TypeScript compiles without errors: `cd app && npx tsc --noEmit`</verify>
  <done>Schema file exists with acceptTerms and acceptNoPhiPolicy fields requiring true values</done>
</task>

<task type="auto">
  <name>Task 2: Add legal checkboxes to signup form</name>
  <files>app/app/signup/page.tsx</files>
  <action>
    Update the signup page to:

    1. Import the schema from `@/lib/validation/signup-schema`
    2. Remove the inline schema definition
    3. Add two checkbox fields to formData state: `acceptTerms: false`, `acceptNoPhiPolicy: false`
    4. Add checkbox UI between the Confirm Password field and the Submit button

    Checkbox 1 (acceptTerms):
    - Label: "I accept the [Terms of Service](link) and understand this tool is provided as-is with no warranties"
    - Link the "Terms of Service" text to /terms (opens in new tab)
    - Checkbox uses existing Checkbox component from @/components/ui/checkbox
    - Show validation error below if not checked on submit

    Checkbox 2 (acceptNoPhiPolicy):
    - Label: "I understand I must NOT upload patient-identifiable or personal information"
    - No link needed
    - Show validation error below if not checked on submit

    Styling:
    - Use FadeIn wrapper for each checkbox group (match existing pattern)
    - Checkbox labels use `text-sm text-text-secondary`
    - Links use `text-brand hover:underline`
    - Error messages use same pattern as other field errors

    Accessibility:
    - Checkboxes have proper aria-invalid and aria-describedby
    - Links open in new tab with rel="noopener noreferrer"
  </action>
  <verify>
    1. Run `cd app && npm run build` - build succeeds
    2. Visual check: Start dev server, visit /signup, verify checkboxes appear
    3. Functional check: Try to submit without checking boxes - should show errors
  </verify>
  <done>
    - Signup form has two mandatory checkboxes before submit button
    - Form cannot be submitted unless both checkboxes are checked
    - Validation errors display when checkboxes not checked
    - Terms link navigates to /terms
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm run build` succeeds without errors
- [ ] Signup page renders both checkboxes
- [ ] Form submission blocked when checkboxes unchecked
- [ ] Error messages appear for unchecked checkboxes
- [ ] Terms of Service link opens /terms in new tab
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Requirements GATE-01 and GATE-02 satisfied
- Form validation prevents submission without consent
  </success_criteria>

<output>
After completion, create `.planning/phases/22-sign-up-acknowledgment/22-01-SUMMARY.md`
</output>
